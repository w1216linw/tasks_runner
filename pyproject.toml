[project]
name = "taskrunner"
version = "0.1.0"
description = "running some scripts with excels"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nicegui",
    "openpyxl",
    "pandas",
    "pgeocode",
    "matplotlib",
]
