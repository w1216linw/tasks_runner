[project]
name = "taskrunner"
version = "1.5.1"
description = "running some scripts with excels"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nicegui",
    "openpyxl",
    "pandas",
    "pgeocode",
    "matplotlib",
]
